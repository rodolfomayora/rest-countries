import React, { FC } from 'react';
import { Layout, Container } from '../../components';

const Home: FC = () => (
  <Layout>
    <main>
      <Container>
        <h1>home</h1>
      </Container>
    </main>
  </Layout>
);

export default Home;